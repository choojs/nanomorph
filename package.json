{
  "name": "nanodiff",
  "version": "1.0.0",
  "description": "Embeddable DOM diffing algorithm",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js",
    "build": "browserify globals.js -o dist/nanodiff.js"
  },
  "repository": "yoshuawuyts/nanodiff",
  "keywords": [
    "dom",
    "diff",
    "tiny",
    "morphdom",
    "choo",
    "bel",
    "yo-yo"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^13.0.1",
    "dependency-check": "^2.5.1",
    "istanbul": "^0.4.4",
    "standard": "^7.1.2",
    "tape": "^4.6.0"
  }
}
